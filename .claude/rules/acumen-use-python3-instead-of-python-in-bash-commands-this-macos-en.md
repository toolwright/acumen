# Acumen insight

Use python3 instead of python in Bash commands -- this macOS environment does not have python on PATH, causing exit code 127 (command not found). Observed across multiple sessions with 5 total Bash tool_failure errors.
